# GuiaGastronomicaHuelva

Starter repository for the "Guía Gastronómica Huelva" project.

This repo was created on GitHub and cloned locally.

## Status

Empty repository — initial README, .gitignore and LICENSE added.

## Next steps

- Add source code for the site or app.
- Add a project manifest (e.g., `package.json` for Node, `pyproject.toml` or `requirements.txt` for Python).

## How to contribute

1. Create a branch: `git checkout -b feature/your-feature`.
2. Make changes and commit.
3. Push and open a pull request on GitHub.

## Local setup (example)

Add a language-specific manifest and update this README with exact steps to run or build the project.
